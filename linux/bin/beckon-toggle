#!/bin/bash
# Toggle beckon: start if not running, stop if running
PIDFILE="$XDG_RUNTIME_DIR/beckon/live.pid"
if [ -f "$PIDFILE" ] && kill -0 "$(cat "$PIDFILE")" 2>/dev/null; then
    kill "$(cat "$PIDFILE")" 2>/dev/null
    rm -f "$PIDFILE"
else
    exec beckon
fi
